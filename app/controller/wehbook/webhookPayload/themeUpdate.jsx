
export const themeUpdate = async (admin, session, shop, payload) => {
    try {
        console.log("theme update payload-------", JSON.stringify(payload, null, 2))
    } catch (err) {
        console.log("theme update webhook error-----------")
    }
}